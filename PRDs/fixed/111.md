```
# 任务：复刻 A 股每日分析报告输出系统

你需要在本地项目：

D:\code\stock-ai-system

中实现一个 A 股每日分析系统。目标是生成一份类似当前截图中的日报，输出结构包括：

1. 数据获取完成
2. A股日报 · 大盘总览
3. 行业板块分析
4. 概念板块分析
5. 情绪周期分析
6. 选股池：一次起爆
7. 选股池：N字异动
8. 选股池：板块联动
9. 选股池：趋势强弱
10. 风险与机会
11. 市场总结与策略
12. 明日策略与建议

要求：
- 使用 Python 编写；
- 数据源优先使用 AkShare；
- 使用 pandas 做数据处理；
- 报告输出为 Markdown 文本；
- 后续可推送飞书，但先保证本地生成报告；
- 不允许凭空生成行情数据，所有数据必须来自 AkShare、数据库或程序计算；
- 如果某些数据接口不可用，要有 fallback，不要直接崩溃；
- 所有策略只做数据分析，不构成投资建议。

---

# 一、建议项目结构

请按下面结构组织代码：

stock-ai-system/
│
├── analysis/
│   ├── __init__.py
│   ├── daily_report.py          # 每日分析主入口
│   ├── data_fetcher.py          # 数据获取
│   ├── market.py                # 大盘分析
│   ├── board.py                 # 行业/概念板块分析
│   ├── sentiment.py             # 情绪周期分析
│   ├── selector.py              # 选股策略
│   ├── report_renderer.py       # 报告文本渲染
│   ├── tracker.py               # 个股跟踪，可先留接口
│   └── utils.py                 # 工具函数
│
├── data/
│   ├── __init__.py
│   └── config.py                # 配置，数据库 DSN、路径等
│
├── reports/
│   └── daily/
│
└── requirements.txt

---

# 二、核心入口

## 1. 每日分析入口

文件：analysis/daily_report.py

运行命令：

```bash
cd D:\project\stock-ai-system
python -m analysis.daily_report
```

主流程：

```
from datetime import datetime
from analysis.data_fetcher import (
    get_trade_date,
    is_trade_day,
    fetch_stock_spot,
    fetch_index_spot,
    fetch_industry_boards,
    fetch_concept_boards,
    enrich_stock_indicators,
)
from analysis.market import analyze_market
from analysis.board import analyze_boards
from analysis.sentiment import analyze_sentiment
from analysis.selector import run_all_selectors
from analysis.report_renderer import render_daily_report, save_report


def main():
    trade_date = get_trade_date()

    if not is_trade_day(trade_date):
        print(f"{trade_date} 非交易日，跳过分析")
        return

    print(f"开始获取数据：{trade_date}")

    stock_df = fetch_stock_spot()
    index_df = fetch_index_spot()
    industry_df = fetch_industry_boards()
    concept_df = fetch_concept_boards()

    stock_df = enrich_stock_indicators(stock_df)

    data_status = {
        "trade_date": trade_date,
        "stock_count": len(stock_df),
        "industry_count": len(industry_df),
        "concept_count": len(concept_df),
    }

    market_result = analyze_market(stock_df, index_df)
    industry_result = analyze_boards(industry_df, board_type="行业")
    concept_result = analyze_boards(concept_df, board_type="概念")
    sentiment_result = analyze_sentiment(
        stock_df=stock_df,
        industry_df=industry_df,
        concept_df=concept_df,
    )

    selector_result = run_all_selectors(
        stock_df=stock_df,
        industry_df=industry_df,
        concept_df=concept_df,
    )

    report = render_daily_report(
        trade_date=trade_date,
        data_status=data_status,
        market=market_result,
        industry=industry_result,
        concept=concept_result,
        sentiment=sentiment_result,
        selectors=selector_result,
    )

    path = save_report(report, trade_date)
    print(report)
    print(f"报告已保存：{path}")


if __name__ == "__main__":
    main()
```

------

# 三、数据获取模块

文件：analysis/data_fetcher.py

目标：获取 A 股个股、指数、行业、概念板块数据。

```
import pandas as pd
import numpy as np
from datetime import datetime
import akshare as ak


def get_trade_date():
    return datetime.now().strftime("%Y%m%d")


def is_trade_day(trade_date: str) -> bool:
    try:
        cal = ak.tool_trade_date_hist_sina()
        cal["trade_date"] = pd.to_datetime(cal["trade_date"]).dt.strftime("%Y%m%d")
        return trade_date in set(cal["trade_date"])
    except Exception:
        # 如果交易日接口失败，默认继续运行，避免日报无法生成
        return True


def safe_numeric(df, cols):
    for col in cols:
        if col in df.columns:
            df[col] = pd.to_numeric(df[col], errors="coerce")
    return df


def fetch_stock_spot():
    """
    获取 A 股实时行情。
    AkShare 常用接口：
    ak.stock_zh_a_spot_em()
    """
    df = ak.stock_zh_a_spot_em()

    # 常见字段：
    # 代码、名称、最新价、涨跌幅、涨跌额、成交量、成交额、振幅、最高、最低、今开、昨收、量比、换手率、市盈率-动态、市净率、总市值、流通市值、涨速、5分钟涨跌、60日涨跌幅、年初至今涨跌幅

    rename_map = {
        "代码": "code",
        "名称": "name",
        "最新价": "close",
        "涨跌幅": "pct_chg",
        "成交量": "volume",
        "成交额": "amount",
        "振幅": "amplitude",
        "最高": "high",
        "最低": "low",
        "今开": "open",
        "昨收": "pre_close",
        "量比": "volume_ratio",
        "换手率": "turnover",
        "市盈率-动态": "pe",
        "市净率": "pb",
        "总市值": "total_mv",
        "流通市值": "float_mv",
    }

    df = df.rename(columns=rename_map)

    numeric_cols = [
        "close", "pct_chg", "volume", "amount", "amplitude",
        "high", "low", "open", "pre_close", "volume_ratio",
        "turnover", "pe", "pb", "total_mv", "float_mv"
    ]
    df = safe_numeric(df, numeric_cols)

    df = df.dropna(subset=["code", "name", "close", "pct_chg"])
    return df


def fetch_index_spot():
    """
    获取主要指数。
    """
    try:
        df = ak.stock_zh_index_spot_em()
    except Exception:
        df = pd.DataFrame()

    return df


def fetch_industry_boards():
    """
    获取行业板块。
    """
    df = ak.stock_board_industry_name_em()

    rename_map = {
        "板块名称": "board_name",
        "最新价": "price",
        "涨跌幅": "pct_chg",
        "总市值": "total_mv",
        "换手率": "turnover",
        "上涨家数": "up_count",
        "下跌家数": "down_count",
        "领涨股票": "leader",
        "领涨股票-涨跌幅": "leader_pct_chg",
    }
    df = df.rename(columns=rename_map)
    df["board_type"] = "行业"

    numeric_cols = [
        "price", "pct_chg", "total_mv", "turnover",
        "up_count", "down_count", "leader_pct_chg"
    ]
    df = safe_numeric(df, numeric_cols)

    # 如果没有成交额字段，用 total_mv 做弱替代；最好后续接板块成交额历史表
    if "amount" not in df.columns:
        df["amount"] = np.nan

    return df


def fetch_concept_boards():
    """
    获取概念板块。
    """
    df = ak.stock_board_concept_name_em()

    rename_map = {
        "板块名称": "board_name",
        "最新价": "price",
        "涨跌幅": "pct_chg",
        "总市值": "total_mv",
        "换手率": "turnover",
        "上涨家数": "up_count",
        "下跌家数": "down_count",
        "领涨股票": "leader",
        "领涨股票-涨跌幅": "leader_pct_chg",
    }
    df = df.rename(columns=rename_map)
    df["board_type"] = "概念"

    numeric_cols = [
        "price", "pct_chg", "total_mv", "turnover",
        "up_count", "down_count", "leader_pct_chg"
    ]
    df = safe_numeric(df, numeric_cols)

    if "amount" not in df.columns:
        df["amount"] = np.nan

    return df


def get_stock_history(code: str, days: int = 80):
    """
    获取个股历史行情，用于计算 MA5/MA10/MA20、5日涨幅、20日涨幅。
    """
    try:
        symbol = code
        df = ak.stock_zh_a_hist(
            symbol=symbol,
            period="daily",
            adjust="qfq"
        )
        df = df.tail(days).copy()

        rename_map = {
            "日期": "date",
            "开盘": "open",
            "收盘": "close",
            "最高": "high",
            "最低": "low",
            "成交量": "volume",
            "成交额": "amount",
            "涨跌幅": "pct_chg",
            "换手率": "turnover",
        }
        df = df.rename(columns=rename_map)
        df = safe_numeric(df, ["open", "close", "high", "low", "volume", "amount", "pct_chg", "turnover"])
        return df
    except Exception:
        return pd.DataFrame()


def enrich_stock_indicators(stock_df):
    """
    给个股实时数据补充 MA5、MA10、MA20、5日涨幅、20日涨幅。
    为了避免太慢，可以先只对涨幅靠前、成交额靠前的股票计算。
    """
    df = stock_df.copy()

    df["ma5"] = np.nan
    df["ma10"] = np.nan
    df["ma20"] = np.nan
    df["pct_5d"] = np.nan
    df["pct_20d"] = np.nan

    # 为了性能，只给可能入选的股票算历史指标
    candidates = df[
        (df["amount"] > 100000000)
        & (df["close"] > 2)
        & (df["pct_chg"] > -8)
    ].copy()

    # 最多计算前 500 只，防止太慢
    candidates = candidates.sort_values("amount", ascending=False).head(500)

    for idx, row in candidates.iterrows():
        hist = get_stock_history(row["code"], days=80)
        if hist.empty or len(hist) < 20:
            continue

        close = hist["close"]
        ma5 = close.tail(5).mean()
        ma10 = close.tail(10).mean()
        ma20 = close.tail(20).mean()

        pct_5d = close.iloc[-1] / close.iloc[-6] - 1 if len(close) >= 6 else np.nan
        pct_20d = close.iloc[-1] / close.iloc[-21] - 1 if len(close) >= 21 else np.nan

        df.loc[idx, "ma5"] = ma5
        df.loc[idx, "ma10"] = ma10
        df.loc[idx, "ma20"] = ma20
        df.loc[idx, "pct_5d"] = pct_5d * 100
        df.loc[idx, "pct_20d"] = pct_20d * 100

    return df
```

------

# 四、大盘分析模块

文件：analysis/market.py

目标：生成截图中类似“大盘总览”的内容。

需要输出：

- 上证指数、深证成指、创业板指；
- 成交额；
- 上涨/下跌/平盘数量；
- 涨停/跌停；
- 20cm 涨停/跌停；
- 市场情绪评分；
- 简短解读。

```
import numpy as np
import pandas as pd


def get_main_indices(index_df):
    result = []

    if index_df is None or index_df.empty:
        return result

    name_col = "名称" if "名称" in index_df.columns else None
    code_col = "代码" if "代码" in index_df.columns else None

    target_names = ["上证指数", "深证成指", "创业板指"]

    for name in target_names:
        row = None

        if name_col:
            matched = index_df[index_df[name_col].astype(str).str.contains(name, na=False)]
            if not matched.empty:
                row = matched.iloc[0]

        if row is not None:
            result.append({
                "name": name,
                "close": row.get("最新价", row.get("close", np.nan)),
                "pct_chg": row.get("涨跌幅", row.get("pct_chg", np.nan)),
                "amount": row.get("成交额", row.get("amount", np.nan)),
                "high": row.get("最高", np.nan),
                "low": row.get("最低", np.nan),
                "open": row.get("今开", np.nan),
            })

    return result


def classify_market_status(score):
    if score >= 75:
        return "强势"
    elif score >= 60:
        return "偏强"
    elif score >= 45:
        return "平衡"
    elif score >= 30:
        return "偏弱"
    else:
        return "弱势"


def analyze_market(stock_df, index_df):
    df = stock_df.copy()

    up_count = int((df["pct_chg"] > 0).sum())
    down_count = int((df["pct_chg"] < 0).sum())
    flat_count = int((df["pct_chg"] == 0).sum())

    limit_up = int((df["pct_chg"] >= 9.8).sum())
    limit_down = int((df["pct_chg"] <= -9.8).sum())

    limit_up_20cm = int((df["pct_chg"] >= 19.5).sum())
    limit_down_20cm = int((df["pct_chg"] <= -19.5).sum())

    total_amount = df["amount"].sum() / 1e8

    up_ratio = up_count / max(len(df), 1)
    limit_up_ratio = limit_up / max(len(df), 1)
    down_ratio = down_count / max(len(df), 1)

    # 简单市场情绪评分，0-100
    score = (
        up_ratio * 45
        + min(limit_up / 80, 1) * 25
        + min(total_amount / 12000, 1) * 20
        - min(limit_down / 50, 1) * 15
        + 25
    )
    score = max(0, min(100, score))

    status = classify_market_status(score)

    indices = get_main_indices(index_df)

    if score >= 60:
        summary = "市场情绪偏强，赚钱效应相对活跃，适合关注主线方向的分歧低吸。"
    elif score >= 45:
        summary = "市场处于震荡平衡状态，板块轮动较快，操作上应控制追高。"
    else:
        summary = "市场情绪偏弱，亏钱效应较明显，应降低仓位并等待情绪修复。"

    return {
        "indices": indices,
        "total_amount": total_amount,
        "up_count": up_count,
        "down_count": down_count,
        "flat_count": flat_count,
        "limit_up": limit_up,
        "limit_down": limit_down,
        "limit_up_20cm": limit_up_20cm,
        "limit_down_20cm": limit_down_20cm,
        "score": round(score, 1),
        "status": status,
        "summary": summary,
    }
```

------

# 五、行业/概念板块分析模块

文件：analysis/board.py

目标：复刻截图中的：

- 行业涨幅 TOP10
- 行业跌幅 TOP10
- 行业成交占比 3 日递增 TOP10
- 行业成交占比 3 日递减 TOP10
- 行业 5 日成交占比上升 TOP10
- 行业 5 日成交占比下降 TOP10
- 概念板块同理

如果暂时没有历史数据库，就先用当日涨跌幅、换手率、领涨股涨幅构造强度分。

```
import numpy as np
import pandas as pd


def add_board_strength(df):
    df = df.copy()

    for col in ["pct_chg", "turnover", "leader_pct_chg", "up_count", "down_count"]:
        if col not in df.columns:
            df[col] = np.nan

    total_members = df["up_count"].fillna(0) + df["down_count"].fillna(0)
    df["up_ratio"] = df["up_count"].fillna(0) / total_members.replace(0, np.nan)
    df["up_ratio"] = df["up_ratio"].fillna(0)

    df["strength_score"] = (
        df["pct_chg"].fillna(0) * 0.45
        + df["turnover"].fillna(0) * 0.20
        + df["leader_pct_chg"].fillna(0) * 0.20
        + df["up_ratio"].fillna(0) * 10 * 0.15
    )

    return df


def analyze_boards(board_df, board_type="行业"):
    df = board_df.copy()
    df = add_board_strength(df)

    top_gain = df.sort_values("pct_chg", ascending=False).head(10)
    top_loss = df.sort_values("pct_chg", ascending=True).head(10)
    top_strength = df.sort_values("strength_score", ascending=False).head(10)

    # 如果未来有历史成交占比表，可以替换这里
    # 当前先用 turnover 变化的近似逻辑占位
    top_hot = df.sort_values(["turnover", "pct_chg"], ascending=False).head(10)
    top_cold = df.sort_values(["pct_chg", "turnover"], ascending=True).head(10)

    active_boards = top_strength["board_name"].head(5).tolist()
    weak_boards = top_loss["board_name"].head(5).tolist()

    return {
        "board_type": board_type,
        "top_gain": top_gain,
        "top_loss": top_loss,
        "top_strength": top_strength,
        "top_hot": top_hot,
        "top_cold": top_cold,
        "active_boards": active_boards,
        "weak_boards": weak_boards,
    }
```

------

# 六、情绪周期分析模块

文件：analysis/sentiment.py

目标：复刻截图中的“市场情绪温度计”“行业板块情绪分布”“概念板块情绪分布”。

```
import pandas as pd
import numpy as np


def classify_emotion(row):
    pct = row.get("pct_chg", 0)
    score = row.get("strength_score", pct)

    if pct >= 5 or score >= 6:
        return "高潮"
    elif pct >= 2:
        return "过热"
    elif pct >= 0:
        return "平衡"
    elif pct >= -2:
        return "退潮"
    else:
        return "冰点"


def distribution(df):
    if df is None or df.empty:
        return {}

    temp = df.copy()
    if "strength_score" not in temp.columns:
        temp["strength_score"] = temp.get("pct_chg", 0)

    temp["emotion"] = temp.apply(classify_emotion, axis=1)

    order = ["高潮", "过热", "平衡", "退潮", "冰点"]
    result = {}

    total = len(temp)
    for label in order:
        count = int((temp["emotion"] == label).sum())
        pct = count / max(total, 1) * 100
        result[label] = {
            "count": count,
            "ratio": round(pct, 1),
        }

    return result


def analyze_sentiment(stock_df, industry_df, concept_df):
    up_count = int((stock_df["pct_chg"] > 0).sum())
    down_count = int((stock_df["pct_chg"] < 0).sum())
    limit_up = int((stock_df["pct_chg"] >= 9.8).sum())
    limit_down = int((stock_df["pct_chg"] <= -9.8).sum())
    amount = stock_df["amount"].sum() / 1e8

    raw_score = (
        up_count / max(len(stock_df), 1) * 40
        + min(limit_up / 80, 1) * 30
        + min(amount / 12000, 1) * 20
        - min(limit_down / 50, 1) * 20
        + 20
    )

    score = max(0, min(100, raw_score))

    if score >= 75:
        stage = "高潮"
        comment = "市场情绪处于高潮区，注意高位分歧和次日兑现压力。"
    elif score >= 60:
        stage = "过热"
        comment = "市场情绪较热，主线方向仍有机会，但追高风险上升。"
    elif score >= 45:
        stage = "平衡"
        comment = "市场处于平衡震荡阶段，资金轮动较快。"
    elif score >= 30:
        stage = "退潮"
        comment = "市场处于退潮阶段，需等待亏钱效应释放。"
    else:
        stage = "冰点"
        comment = "市场情绪接近冰点，短线风险较高，但也可能孕育修复机会。"

    return {
        "score": round(score, 1),
        "stage": stage,
        "comment": comment,
        "industry_distribution": distribution(industry_df),
        "concept_distribution": distribution(concept_df),
    }
```

------

# 七、选股策略模块

文件：analysis/selector.py

目标：生成截图里的几个选股池：

1. 一次起爆
2. N字异动
3. 板块联动
4. 趋势强弱

每只个股输出：

- 代码
- 名称
- 收盘价
- 涨幅
- 量比
- MA5 / MA10 / MA20
- 5日涨幅 / 20日涨幅
- 买入价区间
- 目标价
- 止损价
- 持仓周期
- 仓位
- 推荐理由

```
import pandas as pd
import numpy as np
import argparse


def calc_trade_plan(row, style="short"):
    close = row["close"]

    if style == "aggressive":
        buy_low = close * 0.98
        buy_high = close * 1.02
        target = close * 1.10
        stop = close * 0.95
        hold_days = "2-5日"
    elif style == "latent":
        buy_low = close * 0.96
        buy_high = close * 1.00
        target = close * 1.15
        stop = close * 0.92
        hold_days = "5-10日"
    else:
        buy_low = close * 0.97
        buy_high = close * 1.01
        target = close * 1.10
        stop = close * 0.95
        hold_days = "3-5日"

    return {
        "buy_low": round(buy_low, 2),
        "buy_high": round(buy_high, 2),
        "target": round(target, 2),
        "stop_loss": round(stop, 2),
        "hold_days": hold_days,
    }


def add_common_fields(df, strategy_name, style="short"):
    rows = []

    for _, row in df.iterrows():
        plan = calc_trade_plan(row, style=style)

        reason_parts = []

        if row.get("pct_chg", 0) > 5:
            reason_parts.append(f"今日涨幅{row.get('pct_chg', 0):.1f}%，短线资金关注")
        if row.get("volume_ratio", 0) > 1.5:
            reason_parts.append(f"量比{row.get('volume_ratio', 0):.2f}，存在放量")
        if row.get("ma5", np.nan) and row.get("close", 0) > row.get("ma5", np.inf):
            reason_parts.append("收盘价站上MA5")
        if row.get("ma20", np.nan) and row.get("close", 0) > row.get("ma20", np.inf):
            reason_parts.append("趋势保持在MA20上方")
        if row.get("pct_20d", 0) > 10:
            reason_parts.append(f"20日涨幅{row.get('pct_20d', 0):.1f}%，趋势活跃")

        if not reason_parts:
            reason_parts.append("技术形态符合策略筛选条件")

        rows.append({
            "strategy": strategy_name,
            "code": row.get("code"),
            "name": row.get("name"),
            "close": round(row.get("close", np.nan), 2),
            "pct_chg": round(row.get("pct_chg", np.nan), 2),
            "volume_ratio": round(row.get("volume_ratio", np.nan), 2),
            "ma5": round(row.get("ma5", np.nan), 2) if pd.notna(row.get("ma5", np.nan)) else np.nan,
            "ma10": round(row.get("ma10", np.nan), 2) if pd.notna(row.get("ma10", np.nan)) else np.nan,
            "ma20": round(row.get("ma20", np.nan), 2) if pd.notna(row.get("ma20", np.nan)) else np.nan,
            "pct_5d": round(row.get("pct_5d", np.nan), 1) if pd.notna(row.get("pct_5d", np.nan)) else np.nan,
            "pct_20d": round(row.get("pct_20d", np.nan), 1) if pd.notna(row.get("pct_20d", np.nan)) else np.nan,
            "buy_low": plan["buy_low"],
            "buy_high": plan["buy_high"],
            "target": plan["target"],
            "stop_loss": plan["stop_loss"],
            "hold_days": plan["hold_days"],
            "position": "1/5仓",
            "reason": "，".join(reason_parts),
        })

    return pd.DataFrame(rows)


def filter_common_stock_pool(stock_df):
    df = stock_df.copy()

    # 排除 ST、退市、北交所、价格异常、成交额过低
    df = df[~df["name"].astype(str).str.contains("ST|退", na=False)]
    df = df[~df["code"].astype(str).str.startswith(("8", "4"))]
    df = df[df["close"] > 2]
    df = df[df["amount"] > 100000000]

    return df


def select_first_breakout(stock_df, limit=5):
    """
    一次起爆：
    - 今日涨幅较高；
    - 放量；
    - 收盘站上短期均线；
    - 5日涨幅不能过高，避免过度追高。
    """
    df = filter_common_stock_pool(stock_df)

    cond = (
        (df["pct_chg"] >= 3)
        & (df["pct_chg"] <= 15)
        & (df["volume_ratio"] >= 1.2)
        & (df["close"] >= df["ma5"].fillna(df["close"]))
        & (df["amount"] >= 200000000)
    )

    result = df[cond].copy()

    result["score"] = (
        result["pct_chg"].fillna(0) * 0.4
        + result["volume_ratio"].fillna(0) * 2
        + result["turnover"].fillna(0) * 0.2
        - result["pct_5d"].fillna(0).clip(lower=0) * 0.05
    )

    result = result.sort_values("score", ascending=False).head(limit)
    return add_common_fields(result, "一次起爆", style="aggressive")


def select_n_latent(stock_df, limit=5):
    """
    N字异动/低吸潜伏：
    - 20日趋势有涨幅；
    - 近期有回调或涨幅不极端；
    - 价格仍在 MA20 附近或上方；
    - 量比不过分夸张。
    """
    df = filter_common_stock_pool(stock_df)

    cond = (
        (df["pct_20d"].fillna(0) >= 8)
        & (df["pct_5d"].fillna(0) <= 12)
        & (df["pct_chg"] > -4)
        & (df["close"] >= df["ma20"].fillna(df["close"]) * 0.97)
        & (df["volume_ratio"].fillna(1) >= 0.6)
    )

    result = df[cond].copy()

    result["score"] = (
        result["pct_20d"].fillna(0) * 0.35
        - result["pct_5d"].fillna(0) * 0.15
        + result["volume_ratio"].fillna(1) * 1.5
        + result["turnover"].fillna(0) * 0.1
    )

    result = result.sort_values("score", ascending=False).head(limit)
    return add_common_fields(result, "N字异动", style="latent")


def select_n_breakout(stock_df, limit=5):
    """
    二次起爆：
    - 前期有趋势；
    - 今日重新放量上涨；
    - 站上 MA5/MA10；
    - 20日涨幅较好。
    """
    df = filter_common_stock_pool(stock_df)

    cond = (
        (df["pct_chg"] >= 2)
        & (df["volume_ratio"] >= 1.3)
        & (df["pct_20d"].fillna(0) >= 10)
        & (df["close"] >= df["ma5"].fillna(df["close"]))
        & (df["close"] >= df["ma10"].fillna(df["close"]))
    )

    result = df[cond].copy()

    result["score"] = (
        result["pct_chg"].fillna(0) * 0.3
        + result["pct_20d"].fillna(0) * 0.3
        + result["volume_ratio"].fillna(0) * 2
        + result["turnover"].fillna(0) * 0.15
    )

    result = result.sort_values("score", ascending=False).head(limit)
    return add_common_fields(result, "二次起爆", style="aggressive")


def select_short_strong(stock_df, limit=5):
    """
    短线强势：
    - 今日涨幅强；
    - 量比高；
    - 成交额充足；
    - 多头排列。
    """
    df = filter_common_stock_pool(stock_df)

    cond = (
        (df["pct_chg"] >= 5)
        & (df["volume_ratio"] >= 1.5)
        & (df["amount"] >= 300000000)
        & (df["close"] >= df["ma5"].fillna(df["close"]))
        & (df["ma5"].fillna(0) >= df["ma10"].fillna(0))
    )

    result = df[cond].copy()

    result["score"] = (
        result["pct_chg"].fillna(0) * 0.5
        + result["volume_ratio"].fillna(0) * 2
        + result["turnover"].fillna(0) * 0.2
    )

    result = result.sort_values("score", ascending=False).head(limit)
    return add_common_fields(result, "短线强势", style="aggressive")


def select_board_linkage(stock_df, industry_df, concept_df, limit=5):
    """
    板块联动：
    简化版本：先取强势板块名称，再在个股名称/板块映射不可用时，用强势个股代替。
    如果后续有个股所属行业/概念字段，应改成：
    所属行业 in 强势行业 或 所属概念 in 强势概念。
    """
    df = filter_common_stock_pool(stock_df)

    cond = (
        (df["pct_chg"] >= 4)
        & (df["volume_ratio"] >= 1.2)
        & (df["amount"] >= 200000000)
    )

    result = df[cond].copy()

    result["score"] = (
        result["pct_chg"].fillna(0) * 0.45
        + result["volume_ratio"].fillna(0) * 2
        + result["amount"].fillna(0) / 1e8 * 0.05
    )

    result = result.sort_values("score", ascending=False).head(limit)
    return add_common_fields(result, "板块联动", style="short")


def run_all_selectors(stock_df, industry_df=None, concept_df=None):
    first = select_first_breakout(stock_df, limit=5)
    n_latent = select_n_latent(stock_df, limit=5)
    n_breakout = select_n_breakout(stock_df, limit=5)
    board_linkage = select_board_linkage(stock_df, industry_df, concept_df, limit=5)
    short_strong = select_short_strong(stock_df, limit=5)

    return {
        "一次起爆": first,
        "N字异动": n_latent,
        "二次起爆": n_breakout,
        "板块联动": board_linkage,
        "短线强势": short_strong,
    }


def main():
    """
    支持命令：
    python -m analysis.selector --indicator n_latent
    python -m analysis.selector --indicator n_breakout
    python -m analysis.selector --indicator n_latent,n_breakout
    """
    parser = argparse.ArgumentParser()
    parser.add_argument("--indicator", type=str, default="n_latent")
    args = parser.parse_args()

    from analysis.data_fetcher import fetch_stock_spot, enrich_stock_indicators

    stock_df = enrich_stock_indicators(fetch_stock_spot())

    indicators = args.indicator.split(",")

    result_map = {}

    if "n_latent" in indicators:
        result_map["低吸潜伏"] = select_n_latent(stock_df)
    if "n_breakout" in indicators:
        result_map["二次起爆"] = select_n_breakout(stock_df)
    if "short_strong" in indicators:
        result_map["短线强势"] = select_short_strong(stock_df)
    if "midline" in indicators:
        result_map["中线埋伏"] = select_n_latent(stock_df)

    for name, df in result_map.items():
        print(f"\n{name}")
        print(df.to_string(index=False))


if __name__ == "__main__":
    main()
```

------

# 八、报告渲染模块

文件：analysis/report_renderer.py

目标：最终生成和截图类似的 Markdown 报告。

```
from pathlib import Path
from datetime import datetime
import pandas as pd
import numpy as np


def fmt_num(x, digits=2):
    if pd.isna(x):
        return "-"
    return f"{x:.{digits}f}"


def fmt_pct(x, digits=2):
    if pd.isna(x):
        return "-"
    return f"{x:+.{digits}f}%"


def fmt_yi(x, digits=1):
    if pd.isna(x):
        return "-"
    return f"{x:.{digits}f}亿"


def render_board_table(df, cols=None, max_rows=10):
    if df is None or df.empty:
        return "暂无数据\n"

    lines = []

    for i, row in df.head(max_rows).iterrows():
        name = row.get("board_name", "-")
        pct = fmt_pct(row.get("pct_chg", np.nan))
        amount = row.get("amount", np.nan)
        turnover = row.get("turnover", np.nan)
        leader = row.get("leader", "-")
        leader_pct = row.get("leader_pct_chg", np.nan)

        line = (
            f"| {name} | {pct} | "
            f"换手{fmt_num(turnover, 2)} | "
            f"{leader} {fmt_pct(leader_pct)} |"
        )
        lines.append(line)

    return "\n".join(lines) + "\n"


def render_distribution(dist):
    if not dist:
        return "暂无数据\n"

    lines = []
    for label in ["高潮", "过热", "平衡", "退潮", "冰点"]:
        item = dist.get(label, {"count": 0, "ratio": 0})
        bar_len = int(item["ratio"] / 5)
        bar = "█" * bar_len
        lines.append(
            f"| {label} | {item['count']}个 | {item['ratio']}% | {bar} |"
        )

    return "\n".join(lines) + "\n"


def render_stock_pool(df):
    if df is None or df.empty:
        return "暂无符合条件个股\n"

    lines = []

    for idx, row in df.reset_index(drop=True).iterrows():
        lines.append(
            f"{idx + 1}. {row['name']}({row['code']}) — {row['strategy']}"
        )
        lines.append(
            f"收盘：{row['close']} | 涨幅：{fmt_pct(row['pct_chg'])} | 量比：{row['volume_ratio']}"
        )
        lines.append(
            f"MA5：{row['ma5']} | MA10：{row['ma10']} | MA20：{row['ma20']} | "
            f"5日涨幅：{fmt_pct(row['pct_5d'], 1)} | 20日涨幅：{fmt_pct(row['pct_20d'], 1)}"
        )
        lines.append(
            f"买入价：{row['buy_low']}~{row['buy_high']} | "
            f"目标价：{row['target']} | 止损价：{row['stop_loss']}"
        )
        lines.append(
            f"持仓周期：{row['hold_days']} | 仓位：{row['position']}"
        )
        lines.append(
            f"理由：{row['reason']}"
        )
        lines.append("")

    return "\n".join(lines)


def render_market(market):
    lines = []

    lines.append("📊 A股日报 · 大盘总览")
    lines.append("")

    lines.append("### 三大指数")
    indices = market.get("indices", [])

    if indices:
        lines.append("| 指数 | 收盘 | 涨跌幅 | 开盘 | 最高 | 最低 |")
        lines.append("|---|---:|---:|---:|---:|---:|")
        for item in indices:
            lines.append(
                f"| {item['name']} | {fmt_num(item.get('close'))} | "
                f"{fmt_pct(item.get('pct_chg'))} | "
                f"{fmt_num(item.get('open'))} | "
                f"{fmt_num(item.get('high'))} | "
                f"{fmt_num(item.get('low'))} |"
            )
    else:
        lines.append("指数数据暂缺")

    lines.append("")
    lines.append(f"成交额：{market['total_amount']:.0f}亿")
    lines.append("")
    lines.append("### 涨跌统计")
    lines.append(f"- 上涨：{market['up_count']}只")
    lines.append(f"- 下跌：{market['down_count']}只")
    lines.append(f"- 平盘：{market['flat_count']}只")
    lines.append(f"- 涨停：{market['limit_up']}只")
    lines.append(f"- 跌停：{market['limit_down']}只")
    lines.append(f"- 20cm涨停：{market['limit_up_20cm']}只")
    lines.append(f"- 20cm跌停：{market['limit_down_20cm']}只")
    lines.append("")
    lines.append(f"市场情绪评分：{market['score']} / 100，状态：{market['status']}")
    lines.append("")
    lines.append(f"简评：{market['summary']}")
    lines.append("")

    return "\n".join(lines)


def render_daily_report(
    trade_date,
    data_status,
    market,
    industry,
    concept,
    sentiment,
    selectors,
):
    date_display = f"{trade_date[:4]}-{trade_date[4:6]}-{trade_date[6:]}"

    lines = []

    lines.append(f"✅ 数据获取完成 {date_display}")
    lines.append("")
    lines.append("### 数据下载完成")
    lines.append(f"- 个股日线：{data_status.get('stock_count', 0)}只")
    lines.append(f"- 行业板块：{data_status.get('industry_count', 0)}个")
    lines.append(f"- 概念指数：{data_status.get('concept_count', 0)}条")
    lines.append("")
    lines.append("交易数据已就绪，可以开始分析了。")
    lines.append("")
    lines.append("---")
    lines.append("")

    lines.append(render_market(market))
    lines.append("---")
    lines.append("")

    lines.append(f"🏭 行业板块分析 | {date_display}")
    lines.append("")
    lines.append("### 行业涨幅 TOP10")
    lines.append(render_board_table(industry["top_gain"]))
    lines.append("")
    lines.append("### 行业跌幅 TOP10")
    lines.append(render_board_table(industry["top_loss"]))
    lines.append("")
    lines.append("### 行业强度 TOP10")
    lines.append(render_board_table(industry["top_strength"]))
    lines.append("")
    lines.append("### 行业活跃度 TOP10")
    lines.append(render_board_table(industry["top_hot"]))
    lines.append("")
    lines.append("---")
    lines.append("")

    lines.append(f"🔥 概念板块分析 | {date_display}")
    lines.append("")
    lines.append("### 概念涨幅 TOP10")
    lines.append(render_board_table(concept["top_gain"]))
    lines.append("")
    lines.append("### 概念跌幅 TOP10")
    lines.append(render_board_table(concept["top_loss"]))
    lines.append("")
    lines.append("### 概念强度 TOP10")
    lines.append(render_board_table(concept["top_strength"]))
    lines.append("")
    lines.append("### 概念活跃度 TOP10")
    lines.append(render_board_table(concept["top_hot"]))
    lines.append("")
    lines.append("---")
    lines.append("")

    lines.append(f"🌡 情绪周期分析 | {date_display}")
    lines.append("")
    lines.append("### 市场情绪温度计")
    lines.append(f"- 情绪评分：{sentiment['score']} / 100")
    lines.append(f"- 当前阶段：{sentiment['stage']}")
    lines.append(f"- 解读：{sentiment['comment']}")
    lines.append("")
    lines.append("### 行业板块情绪分布")
    lines.append(render_distribution(sentiment["industry_distribution"]))
    lines.append("")
    lines.append("### 概念板块情绪分布")
    lines.append(render_distribution(sentiment["concept_distribution"]))
    lines.append("")
    lines.append("---")
    lines.append("")

    lines.append(f"🎯 选股池 · 一次起爆 | {date_display}")
    lines.append("")
    lines.append(render_stock_pool(selectors.get("一次起爆")))
    lines.append("")
    lines.append("---")
    lines.append("")

    lines.append(f"🔍 选股池 · N字异动 | {date_display}")
    lines.append("")
    lines.append(render_stock_pool(selectors.get("N字异动")))
    lines.append("")
    lines.append("---")
    lines.append("")

    lines.append(f"🚀 选股池 · 二次起爆 | {date_display}")
    lines.append("")
    lines.append(render_stock_pool(selectors.get("二次起爆")))
    lines.append("")
    lines.append("---")
    lines.append("")

    lines.append(f"🧩 选股池 · 板块联动 | {date_display}")
    lines.append("")
    lines.append(render_stock_pool(selectors.get("板块联动")))
    lines.append("")
    lines.append("---")
    lines.append("")

    lines.append(f"⚠️ 风险与机会 | {date_display}")
    lines.append("")
    lines.append("### 风险提示")
    lines.append("- 若市场情绪评分低于45，应降低仓位，避免追高。")
    lines.append("- 若跌停数量明显增加，说明亏钱效应扩散。")
    lines.append("- 若热点板块连续高潮，次日应警惕分歧。")
    lines.append("")
    lines.append("### 机会方向")
    lines.append("- 优先关注强势行业与强势概念的交集。")
    lines.append("- 优先选择放量但未严重透支的个股。")
    lines.append("- 优先关注站上 MA5/MA10/MA20 且量价配合的标的。")
    lines.append("")
    lines.append("---")
    lines.append("")

    lines.append(f"📌 市场总结与策略 | {date_display}")
    lines.append("")
    lines.append("一、整体判断")
    lines.append(f"- 当前市场状态：{market['status']}")
    lines.append(f"- 市场情绪评分：{market['score']} / 100")
    lines.append(f"- 情绪周期：{sentiment['stage']}")
    lines.append("")
    lines.append("二、主线方向")
    lines.append("- 参考行业强度 TOP10 与概念强度 TOP10。")
    lines.append("- 若同一方向同时出现在行业和概念强榜中，可视为主线确认度较高。")
    lines.append("")
    lines.append("三、操作建议")
    if market["score"] >= 60:
        lines.append("- 市场情绪偏强，可关注主线板块内的分歧低吸和二次起爆机会。")
        lines.append("- 但连续大涨个股不宜盲目追高。")
    elif market["score"] >= 45:
        lines.append("- 市场处于震荡平衡阶段，适合轻仓试错。")
        lines.append("- 优先关注低吸潜伏和板块联动方向。")
    else:
        lines.append("- 市场偏弱，应控制仓位，减少追高。")
        lines.append("- 等待情绪修复后再提高进攻性。")

    lines.append("")
    lines.append("四、明日策略与建议")
    lines.append("| 场景 | 信号 | 策略 | 仓位 |")
    lines.append("|---|---|---|---|")
    lines.append("| 指数反弹 | 放量上涨且上涨家数增加 | 关注主线低吸 | 3-5成 |")
    lines.append("| 震荡分化 | 热点轮动但成交不足 | 只做核心前排 | 2-3成 |")
    lines.append("| 继续下跌 | 跌停增加且缩量 | 控制风险，等待修复 | 1-2成以下 |")
    lines.append("")
    lines.append("免责声明：以上内容仅为量化数据分析和策略复盘，不构成任何投资建议。")

    return "\n".join(lines)


def save_report(report, trade_date):
    out_dir = Path("reports/daily")
    out_dir.mkdir(parents=True, exist_ok=True)

    path = out_dir / f"daily_report_{trade_date}.md"

    with open(path, "w", encoding="utf-8") as f:
        f.write(report)

    return path
```

------

# 九、配置文件

文件：data/config.py

不要把数据库密码硬编码在 SKILL.md 或代码里。使用环境变量。

```
import os
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parents[1]
REPORT_DIR = BASE_DIR / "reports"

DATABASE_DSN = os.getenv("DATABASE_DSN")
if not DATABASE_DSN:
    raise RuntimeError("DATABASE_DSN 环境变量未设置")

FEISHU_WEBHOOK = os.getenv("FEISHU_WEBHOOK", "")
MINIMAX_API_KEY = os.getenv("MINIMAX_API_KEY", "")
```

------

# 十、requirements.txt

```
akshare
pandas
numpy
requests
python-dotenv
psycopg2-binary
openpyxl
```

------

# 十一、需要重点实现的效果

最终运行：

```
cd D:\project\stock-ai-system
python -m analysis.daily_report
```

应该输出类似：

```
✅ 数据获取完成 2026-05-15

数据下载完成
个股日线：5033只
行业板块：90个
概念指数：365条

📊 A股日报 · 大盘总览
三大指数...
成交额...
上涨...
下跌...
涨停...
跌停...

🏭 行业板块分析
行业涨幅 TOP10
行业跌幅 TOP10
行业强度 TOP10

🔥 概念板块分析
概念涨幅 TOP10
概念跌幅 TOP10
概念强度 TOP10

🌡 情绪周期分析
市场情绪温度计
行业板块情绪分布
概念板块情绪分布

🎯 选股池 · 一次起爆
1. xxx(xxxxxx)
收盘...
买入价...
目标价...
止损价...
理由...

🔍 选股池 · N字异动
...

⚠️ 风险与机会
...

📌 市场总结与策略
...
```

------

# 十二、后续增强方向

第一阶段先完成上面的版本，保证报告能跑通。

第二阶段再补：

1. PostgreSQL 数据库存储；
2. 板块成交占比历史表；
3. 3日/5日成交占比变化；
4. 个股所属行业/概念映射；
5. MiniMax AI 润色总结；
6. 飞书推送；
7. 选股结果加入 20 日跟踪；
8. Excel 导出；
9. 策略回测；
10. 参数配置文件化。

------

# 十三、特别注意

1. 不要生成假的股票数据。
2. 数据接口失败时要返回“数据暂缺”，不要编造。
3. 选股结果必须来自筛选条件。
4. 报告中的买入价、目标价、止损价可以按公式生成，但必须注明这是策略区间，不是投资建议。
5. 如果某个字段 AkShare 没有，就优雅降级。
6. 所有模块都要能单独运行和调试。

```
---

你可以先让 Claude 按这个版本写，基本就能复刻你文档里的日报骨架。  

不过要真正做到和你截图里**一模一样**，还需要补两个关键东西：

第一，**板块成交占比历史表**。你截图里有“3日成交占比递增”“5日成交占比上升”，这必须有历史数据，不是当天接口就能直接算出来。

第二，**个股所属行业/概念映射表**。否则“板块联动选股”只能用近似规则，不能真正判断某只股票是否属于机器
```