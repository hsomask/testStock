一、最推荐的修法：两阶段补均线

不要一开始就给 5000 多只股票全部拉历史 K 线，会慢，也容易被接口限速。

更合理的是：

第一阶段：先给全市场中较重要的候选股票补一批
第二阶段：选股结束后，对最终观察池股票再补一次

也就是说：

enrich_stock_indicators()：补充基础候选池
enrich_selected_stocks_indicators()：补充最终入选观察池

这样速度可控，观察池也不会大量 nan。

二、先修 get_stock_history() 的日志

你现在 _get_stock_history_sina() 和 _get_stock_history_tx() 失败后都是直接返回空表，没有日志。

建议先改成能看到失败原因：

def _get_stock_history_sina(code: str, days: int = 80):
    try:
        df = ak.stock_zh_a_hist(
            symbol=code,
            period="daily",
            adjust="qfq"
        )
        df = df.tail(days).copy()

        rename_map = {
            "日期": "date",
            "开盘": "open",
            "收盘": "close",
            "最高": "high",
            "最低": "low",
            "成交量": "volume",
            "成交额": "amount",
            "涨跌幅": "pct_chg",
            "换手率": "turnover",
        }
        df = df.rename(columns=rename_map)
        df = safe_numeric(df, ["open", "close", "high", "low", "volume", "amount", "pct_chg", "turnover"])
        return df

    except Exception as e:
        logger.debug(f"Sina 历史行情获取失败：{code}, {e}")
        return pd.DataFrame()

腾讯也同理：

except Exception as e:
    logger.debug(f"Tencent 历史行情获取失败：{code}, {e}")
    return pd.DataFrame()

这样你调试时可以把日志级别设成 DEBUG 看具体失败原因。

三、修股票代码兼容：北交所 920xxx 可能是重点

你报告里有很多：

锐翔智能 920178
嘉晨智能 920096
凯德石英 920179

这些北交所股票，AkShare 的 stock_zh_a_hist 不一定都能稳定按 920178 拉到。

建议 get_stock_history() 先尝试多个 symbol 格式：

def get_stock_history(code: str, days: int = 80):
    """
    获取个股历史K线，用于计算 MA/涨幅。
    对不同市场尝试多个代码格式。
    """
    code = str(code).strip()

    symbol_candidates = [code]

    # 部分接口可能需要带市场前缀
    if code.startswith(("6", "9")):
        symbol_candidates.append(f"sh{code}")
    elif code.startswith(("0", "3")):
        symbol_candidates.append(f"sz{code}")
    elif code.startswith(("8", "4")):
        symbol_candidates.append(f"bj{code}")

    for symbol in symbol_candidates:
        df = _get_stock_history_sina(symbol, days)
        if not df.empty:
            return df

    for symbol in symbol_candidates:
        df = _get_stock_history_tx(symbol, days)
        if not df.empty:
            return df

    logger.debug(f"历史行情全部通道失败：{code}, tried={symbol_candidates}")
    return pd.DataFrame()

不过注意：如果 ak.stock_zh_a_hist() 只接受纯数字，带 sh/sz/bj 可能会失败，所以要“多个格式逐个尝试”，不要只改成带前缀。

四、把指标计算封装成函数

现在 enrich_stock_indicators() 里直接计算 MA。建议抽出来：

def calc_history_indicators(hist: pd.DataFrame):
    if hist is None or hist.empty or "close" not in hist.columns:
        return None

    close = pd.to_numeric(hist["close"], errors="coerce").dropna()

    if len(close) < 5:
        return None

    result = {
        "ma5": close.tail(5).mean() if len(close) >= 5 else np.nan,
        "ma10": close.tail(10).mean() if len(close) >= 10 else np.nan,
        "ma20": close.tail(20).mean() if len(close) >= 20 else np.nan,
        "pct_5d": (close.iloc[-1] / close.iloc[-6] - 1) * 100 if len(close) >= 6 else np.nan,
        "pct_20d": (close.iloc[-1] / close.iloc[-21] - 1) * 100 if len(close) >= 21 else np.nan,
    }
    return result
五、扩大第一阶段候选池，不要只取成交额前 500

现在只取前 500 太窄。建议改成：

成交额前 1000
涨幅前 300
量比前 300
换手率前 300
最终去重

这样能覆盖更多观察池候选。

替换你的 enrich_stock_indicators()：

def enrich_stock_indicators(stock_df, max_stocks: int = 1200):
    """给个股实时数据补充 MA5/MA10/MA20/5日涨幅/20日涨幅"""
    df = stock_df.copy()

    for col in ["ma5", "ma10", "ma20", "pct_5d", "pct_20d"]:
        if col not in df.columns:
            df[col] = np.nan

    # 基础过滤
    base = df[
        (pd.to_numeric(df["close"], errors="coerce") > 2)
        & (pd.to_numeric(df["pct_chg"], errors="coerce") > -8)
    ].copy()

    selected_indices = set()

    # 1. 成交额靠前
    if "amount" in base.columns:
        selected_indices.update(
            base.sort_values("amount", ascending=False).head(1000).index.tolist()
        )

    # 2. 涨幅靠前
    selected_indices.update(
        base.sort_values("pct_chg", ascending=False).head(300).index.tolist()
    )

    # 3. 量比靠前
    if "volume_ratio" in base.columns:
        selected_indices.update(
            base.sort_values("volume_ratio", ascending=False).head(300).index.tolist()
        )

    # 4. 换手率靠前
    if "turnover" in base.columns:
        selected_indices.update(
            base.sort_values("turnover", ascending=False).head(300).index.tolist()
        )

    selected_indices = list(selected_indices)[:max_stocks]

    success = 0
    fail = 0

    for idx in selected_indices:
        code = str(df.at[idx, "code"])
        hist = get_stock_history(code, days=80)
        indicators = calc_history_indicators(hist)

        if not indicators:
            fail += 1
            continue

        for k, v in indicators.items():
            df.loc[idx, k] = v

        success += 1

    logger.info(f"技术指标补充完成：成功 {success} 只，失败 {fail} 只，候选 {len(selected_indices)} 只")
    return df

这一步应该能把缺失率从 98% 降下来不少。

六、最关键：选股后再补最终观察池

因为哪怕第一阶段补了 1200 只，最终观察池仍可能有漏网之鱼。所以要新增：

def enrich_selected_stocks_indicators(selector_result):
    """对最终观察池股票二次补齐 MA/涨幅"""
    for pool_name, pool_df in selector_result.items():
        if pool_df is None or pool_df.empty:
            continue

        for idx, row in pool_df.iterrows():
            need_fill = (
                pd.isna(row.get("ma5"))
                or pd.isna(row.get("ma10"))
                or pd.isna(row.get("ma20"))
                or pd.isna(row.get("pct_5d"))
                or pd.isna(row.get("pct_20d"))
            )

            if not need_fill:
                continue

            code = str(row.get("code", ""))
            hist = get_stock_history(code, days=80)
            indicators = calc_history_indicators(hist)

            if not indicators:
                continue

            for k, v in indicators.items():
                pool_df.at[idx, k] = v

        selector_result[pool_name] = pool_df

    return selector_result

然后在 daily_report.py 里，run_all_selectors() 后面加：

from analysis.data_fetcher import enrich_selected_stocks_indicators

selector_result = run_all_selectors(
    stock_df=stock_df,
    industry_df=industry_df,
    concept_df=concept_df,
    market_score=market_score,
)

selector_result = enrich_selected_stocks_indicators(selector_result)

这样报告里的观察池股票会优先补齐。

七、同时修渲染，不要显示 nan

即使有些股票历史接口确实拉不到，也不要在报告里显示：

MA5：nan

应该显示：

MA5：-

这个在 report_renderer.py 里修。

加一个格式化函数：

def fmt_num_or_dash(x, digits=2):
    try:
        if pd.isna(x):
            return "-"
        return f"{float(x):.{digits}f}"
    except Exception:
        return "-"

然后：

MA5：{fmt_num_or_dash(row.get("ma5"))}
MA10：{fmt_num_or_dash(row.get("ma10"))}
MA20：{fmt_num_or_dash(row.get("ma20"))}
八、让数据质量检查分开看“全市场”和“观察池”

现在报告写：

98% 个股缺少均线数据

这个会吓人，但全市场 5000 多只全部补齐本来就很耗时。更关键的是观察池股票有没有补齐。

建议数据质量改成：

全市场均线覆盖率：约 20%
观察池均线覆盖率：约 85%

只要观察池覆盖率高，报告的选股可读性就会明显提升。

九、给 Claude 的完整修复需求

你可以直接复制这段：

# 修复 enrich_stock_indicators 均线数据大量缺失问题

当前报告显示：
- 98% 个股缺少 MA5/MA10/MA20；
- 观察池中大量股票 MA5/MA10/MA20 为 nan；
- 5日涨幅、20日涨幅大量为空。

请按以下方案修复。

## 1. 给历史行情获取增加日志

修改 `_get_stock_history_sina()` 和 `_get_stock_history_tx()`：

- 不要静默失败；
- except 中用 logger.debug 记录 code 和异常；
- get_stock_history() 最终失败时也记录 tried symbols。

## 2. get_stock_history 支持多种代码格式

对 code 生成多个候选 symbol：

- 原始 code；
- 6/9 开头尝试 sh + code；
- 0/3 开头尝试 sz + code；
- 8/4 开头尝试 bj + code；

逐个尝试 Sina，再逐个尝试 Tencent。

不要只用一种格式。

## 3. 抽取 calc_history_indicators(hist)

新增函数：

```python
def calc_history_indicators(hist):
    close = pd.to_numeric(hist["close"], errors="coerce").dropna()
    return {
        "ma5": ...,
        "ma10": ...,
        "ma20": ...,
        "pct_5d": ...,
        "pct_20d": ...,
    }

要求：

close 少于 5 条则返回 None；
NaN 安全处理；
pct_5d / pct_20d 单位为百分数，比如 12.3 表示 +12.3%。
4. 改造 enrich_stock_indicators(stock_df)

不要只补成交额前 500。

改成多维候选池：

成交额前 1000；
涨幅前 300；
量比前 300；
换手率前 300；
合并去重；
最多补 1200 只，可通过参数 max_stocks 控制。

记录日志：
技术指标补充完成：成功 x 只，失败 y 只，候选 z 只。

5. 新增 enrich_selected_stocks_indicators(selector_result)

在最终观察池生成后，对每个池里的股票二次补齐：

如果 ma5/ma10/ma20/pct_5d/pct_20d 任一缺失；
调用 get_stock_history；
调用 calc_history_indicators；
回填到该观察池 DataFrame。
6. daily_report.py 中调用二次补齐

在 run_all_selectors() 后面加：

selector_result = enrich_selected_stocks_indicators(selector_result)

确保最终报告里的观察池股票优先补齐均线。

7. report_renderer.py 不显示 nan

所有 MA、5日涨幅、20日涨幅字段：

NaN 显示为 -
不允许报告中出现 nan
8. data_quality.py 区分全市场和观察池覆盖率

如果方便，新增：

全市场均线缺失率；
观察池均线缺失率。

至少保证观察池均线覆盖率能体现在数据质量说明里。

验收标准

重新运行：

python -m analysis.daily_report --mode both --force

期望：

观察池股票大部分有 MA5/MA10/MA20；
观察池股票大部分有 5日涨幅、20日涨幅；
报告中不再大量出现 MA5：nan；
数据质量分数明显提升；
日志中能看到技术指标补充成功/失败数量。

---

# 十、最小修复版本

如果你不想大改，最少做这两件：

```text
1. enrich_stock_indicators 从前 500 扩大到多维候选 1200；
2. run_all_selectors 后，对最终观察池股票二次补齐。

这两个做完，报告里观察池的 nan 会明显减少。



# 修复 data_quality_log 和 stock_signal 落库问题

当前数据库中：
- data_quality_log 为空；
- stock_signal 为空。

## 1. data_quality_log 为空的原因

`check_data_quality()` 只返回质量检查结果，没有写入 `data_quality_log`。

请在 `daily_report.py` 中新增：

```python
save_data_quality_log(trade_date, quality, data_status, db_conn)

在 quality = check_data_quality(...) 后调用。

写入字段：

trade_date
stock_count
industry_count
concept_count
has_board_amount_ratio
has_stock_board_map
has_3d_history
has_5d_history
ma_missing_ratio
confidence_score
issues

issues 用换行拼接成 TEXT。

2. stock_signal 为空需要加强日志和兼容

当前 save_stock_signals() 已存在，但可能因为表结构旧、唯一约束缺失、NaN 类型或数据库连接异常导致写入失败。

请做以下增强：

2.1 schema.sql 确保唯一约束存在

添加幂等 SQL：

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1 FROM pg_constraint
        WHERE conname = 'uq_stock_signal_trade_code_strategy'
    ) THEN
        ALTER TABLE stock_signal
        ADD CONSTRAINT uq_stock_signal_trade_code_strategy
        UNIQUE (trade_date, code, strategy);
    END IF;
END $$;
2.2 save_stock_signals 增加明确日志

开始写入前打印：

print(f"准备写入 stock_signal：{sum(len(df) for df in selector_result.values() if df is not None)} 条候选")

写入完成打印：

print(f"stock_signal 写入完成：成功 {written} 条，失败 {failed} 条")
2.3 NaN 转 None

新增：

def num_or_none(x):
    try:
        if pd.isna(x):
            return None
        return float(x)
    except Exception:
        return None

所有数值字段写库前都用 num_or_none()。

2.4 单条失败时打印错误原因

当前只 logger.exception，建议同时 print：

print(f"[错误] 写入 stock_signal 失败：{code} {name} {e}")
2.5 如果全部失败，要抛出警告

如果 selector_result 有数据，但 written == 0：

print("[警告] selector_result 有数据，但 stock_signal 未写入任何记录，请检查表结构和数据库权限。")
3. daily_report.py 调用顺序

在 quality = check_data_quality(...) 后：

save_data_quality_log(trade_date, quality, data_status, db_conn)

在 selector_result = run_all_selectors(...) 后：

save_stock_signals(selector_result, trade_date, db_conn)
4. 验收

重新运行：

python -m analysis.daily_report --mode both --force

然后查询：

SELECT COUNT(*) FROM data_quality_log;
SELECT COUNT(*) FROM stock_signal;
SELECT trade_date, code, name, strategy, risk_level, action_signal
FROM stock_signal
ORDER BY created_at DESC
LIMIT 20;

期望：

data_quality_log 至少新增 1 条；
stock_signal 至少新增观察池股票数量对应的记录；
终端看到 stock_signal 写入完成：成功 xx 条，失败 0 条。

---

## 你现在的判断

```text
data_quality_log 空：正常，因为代码还没写入。
stock_signal 空：不正常，说明 dev 没跑到、表约束有问题、连接有问题，或写入失败被日志吞了。